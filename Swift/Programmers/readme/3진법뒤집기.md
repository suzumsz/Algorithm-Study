# 3진법 뒤집기
> 난이도 : Lv1   
> 유형 : 월간 코드 챌린지 시즌1
> 2021-05-22

### Sol
진법으로 변환할 수 있는 `radix`함수로 풀 수 있었다.  
10진수에서 3진수로 변환한 후 수를 뒤집고, 해당 3진수를 10진수 Int 타입으로 최종 변환한다.  
블로그의 도움을 받아 풀 수 있었다.   
ㅜㅜ 더 공부하자 🥲
```Swift
import Foundation
func solution(_ n:Int) -> Int {
    let ternary = String(n, radix: 3)
    return Int(String(ternary.reversed()),radix:3)!
}
```

### Other's Sol
내가 푼 풀이에서 한 줄 코드로 바꿀 수 있었다 !!!
```Swift
import Foundation
func solution(_ n:Int) -> Int {
    return Int(String(String(n, radix: 3).reversed()), radix: 3)!
}
```