# 직사각형 별찍기
> 난이도 : Lv1   
> 유형 : 연습문제  
> 2021-04-18

### Sol
`String(repeating:,count)`를 이용하여 a만큼 *을 찍어주었다.
```Swift
import Foundation

let n = readLine()!.components(separatedBy: [" "]).map { Int($0)! }
let (a, b) = (n[0], n[1])

for _ in 0..<b {
    print(String(repeating: "*", count: a))
}
```
