# 두 개 뽑아서 더하기
> 난이도 : Lv1   
> 유형 : 월간 코드 챌린지 시즌1  
> 2021-05-22

### Sol
두 개를 뽑아서 더해야 하기 때문에 이중 for문을 사용하여 두 개를 비교해주며 더해주었다.  
중복을 제거하기 위해 `Set`함수를 사용하였다.  
오름차순으로 정렬해주기 위해 `sorted()`를 사용하였다.

🔥 파이썬으로 풀고나서 다시 푼 문제인데, 스위프트에 익숙해지기위해 더 노력해야할 것 같다.
```Swift
import Foundation

func solution(_ numbers:[Int]) -> [Int] {
    var answer = Set<Int>()
    for i in 0..<numbers.count { // 0 ~ numbers의 length
        for j in i+1..<numbers.count { 
            answer.insert(numbers[i]+numbers[j])
        }
    }
    return answer.sorted()
}
```